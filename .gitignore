# Local environment variables (contain credentials — do not commit)
# Never hardcode ADMIN_SECRET_KEY — set in server/.env and Railway Variables only
.env
server/.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated test reports (scripts in tests/ are committed)
tests/reports/

# Local admin map dashboard (may store admin key in browser localStorage)
admin/*
!admin/README.md
