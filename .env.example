# Copy this file to .env and fill in your values.
#
# Local dev:
#   VITE_BACKEND_URL=http://YOUR_MAC_LAN_IP:3001
#
# TestFlight / production (must match deployed backend):
#   VITE_BACKEND_URL=https://your-app.up.railway.app
#
# Set before: npm run build && npx cap sync ios
VITE_BACKEND_URL=http://YOUR_MAC_LAN_IP:3001
