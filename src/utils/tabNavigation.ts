/**
 * Shared ref for swipe direction when switching tabs.
 * 1 = swiping left (next tab), -1 = swiping right (prev tab), 0 = no swipe (e.g. bottom nav tap)
 */
export const swipeDirectionRef = { current: 0 };
